#include<iostream>
#include<conio.h>
#include<string>
using namespace std;
int main()
{
	string name; 
	int count = 0;
	cout << "Enter any string : ";
	getline(cin, name);
	int len = name.size();
	for (int i = 0; i < len; i++)
	{
		count++;
	}
	cout << count << endl;

	_getch();
	return 0;
